# Detective Academy EN — audit-polish execution checkpoint

Date: 2026-09-26
Status: bounded final-polish checkpoint; English freeze remains FALSE.

## Current truth

The exact owner-review packet is newer than the stale four-visual gate text on PR #571 / central main. Its release candidate is 146 pages and the exact four owner visuals are already owner-locked and integrated. This does not authorize English freeze, KDP publication, cover selection, pricing, or physical-proof acceptance.

Product source remains at PR #571 head `1fed50b7e969c60da1a1b9d743665473ceb15049`.

## Audit-proven copy defects

Independent text verification of the exact 146-page owner-review PDF found these active US-English normalization residues:

- `enquiry`: 5
- `labelled`: 2
- `theatre`: 1
- `cancelled`: 1
- `travelled`: 1
- `honours`: 1
- `catalogue`: 1
- `animal-centre`: 1
- `practised`: 1

Total: 14 active UK-spelling occurrences. The earlier 13-count checkpoint was incomplete because `practised` was missed.

Case 02 also still says `records a trophy leaving its shelf`, contradicting the locked title `THE TROPHY THAT NEVER REACHED THE SHELF`.

Source verification at the PR head confirms the residues live in `book1_v4_overrides.yml`, `book1_en_phase2.yml`, and `book1_en_phase3.yml`; the Case 02 contradiction is in `book1_v4_overrides.yml`.

## Visual defect boundaries

Page 107 is source-proven: `build_owner_review_v41_finale.py::finale_page_v41` renders seven canonical explanation sections plus routing proof as peer grid cards. This is the exact audited hierarchy defect. The bounded six-hook Detective Six reveal remains the intended presentation-only correction, preserving all seven canonical explanations, routing evidence, debrief, logic, pagination and Room Zero mechanism.

Page 3 is different: `build_owner_review_v4.py::academy_page` simply draws the production `squad.jpg`. `sync_brand_assets.py` derives that file from owner-approved `assets/images/Happy Makers detectives.png`. The old crest is therefore embedded in owner art, not drawn as a separate renderer element. No safe repository-side p.3 correction is authorized until the exact scanner/question-mark replacement asset or owner-approved source is identified. Do not synthesize, redraw, crop or silently alter the locked squad art.

## Implementation attempt

A bounded copy-only source patch plus deterministic regression test was attempted on isolated product branch `rse/detective-final-polish-audit-20260926`. The connected GitHub safety layer blocked the write before mutation. Post-check confirmed the branch remains byte-for-byte identical to PR #571 head: ahead 0 / behind 0.

No bypass was attempted. No product source, owner art, main branch, freeze state or publication state changed.

## Next gate

When an authorized product-source writer is available:

1. Apply only the audit-proven copy replacements, including `practised -> practiced`, and repair Case 02 to state the trophy never reached its shelf.
2. Add a deterministic regression gate over the active EN source layers.
3. Apply the already bounded p.107 six-hook presentation correction.
4. Leave p.3 untouched until exact owner-approved scanner/question-mark source is available.
5. Rebuild the exact 146-page artifact; rerun logic, pagination, grayscale, reverse-entry, typography and full visual QA.
6. Keep English NOT FROZEN pending physical proof and explicit owner freeze.
